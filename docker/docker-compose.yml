version: "3.9"
services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SUMO_HOME=/usr/share/sumo
    volumes:
      - ..:/app
    command: ["uvicorn", "gso.serve.api:app", "--host", "0.0.0.0", "--port", "8000"]
